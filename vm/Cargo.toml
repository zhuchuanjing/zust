[package]
name = "zust-vm"
version = "0.9.74"
edition = "2024"
rust-version = "1.85"
description = "Cranelift JIT runtime for executing Zust modules."
license = "Apache-2.0"
repository = "https://github.com/zhuchuanjing/zust"
homepage = "https://www.zust-lang.com"
keywords = ["zust", "jit", "runtime"]
categories = ["compilers", "development-tools"]

[lib]
name = "vm"

[features]
default = []
ir-disassembly = []
vulkan = ["dep:vulkan", "dep:vulkano"]
metal = ["vm-metal/runtime"]

[dependencies]
log = "0.4.29"
dynamic = { package = "zust-dynamic", version = "0.9.16", path = "../dynamic" }
parser = { package = "zust-parser", version = "0.9.15", path = "../parser" }
compiler = { package = "zust-compiler", version = "0.9.34", path = "../compiler" }
root = { package = "zust-root", version = "0.9.6", path = "../root" }
llm = { package = "zust-llm", version = "0.9.14", path = "../llm" }
vm-spirv = { package = "zust-vm-spirv", version = "0.9.8", path = "../vm-spirv" }
vulkan = { package = "zust-vulkan", version = "0.9.4", path = "../vulkan", optional = true }

anyhow = "1.0.102"
thiserror = "2.0.18"
smol_str = "0.3.6"
bytemuck = "1.25.0"
vulkano = { version = "0.35.2", optional = true }

cranelift = "0.131.1"
cranelift-module = "0.131.1"
cranelift-jit = "0.131.1"

tokio = {version = "1.52.3", features = ["full"]}
uuid = { version = "1.23.1", features = ["v4"]}
rand = "0.10.1"
reqwest = "0.13.3"
base64 = "0.22.1"
axum = { version = "0.8.9", features = ["ws"] }
tower-http = { version = "0.6.10", features = ["cors", "fs"] }
futures-util = "0.3.32"
sqlx-core = { version = "0.8.6", default-features = false, features = ["_rt-tokio", "_tls-rustls-ring-webpki", "any"] }
sqlx-mysql = { version = "0.8.6", default-features = false, features = ["any"] }
sqlx-postgres = { version = "0.8.6", default-features = false, features = ["any"] }

[target.'cfg(target_os = "macos")'.dependencies]
vm-metal = { package = "zust-vm-metal", version = "0.9.9", path = "../vm-metal", default-features = false }

[dev-dependencies]
num-bigint = "0.4.6"
num-traits = "0.2.19"
